// Statically defined types that can be swapped out for more precise, dynamically computed alternatives during the `files:csvtypescript` CI

type TextKeysInterface = string;

/** A type representing asset names (yes this is just a `string` alias). */
type AssetName = string;
